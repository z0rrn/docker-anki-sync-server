name: "Build and push image on native architecture"

# run workflow on every branch, only upload on main (allow manual trigger)
on:
  # build image on push
  push:
    # don't build if only docs changed
    paths-ignore:
      - "**.md"

  pull_request:
  workflow_dispatch:
    inputs:
      debug_enabled:
        description: "Enable SSH access at the end of the workflow"
        type: boolean
        required: false
        default: false

jobs:
  build:
    name: "Build packages"

    runs-on: ubuntu-24.04
    #runs-on: ${{ matrix.os }}
    #strategy:
    #  matrix:
    #    os: [ubuntu-24.04, ubuntu-24.04-arm]

    permissions:
      packages: write

    steps:
      - uses: actions/checkout@v4
      - uses: mikefarah/yq@v4
      - uses: arduino/setup-protoc@v3

      - name: Install Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          target: x86_64-unknown-linux-gnu, aarch64-unknown-linux-gnu
          components: cargo, clippy, rust-docs, rust-std, rustc, rustfmt

      - name: Clone git repository ankitects/anki
        uses: actions/checkout@v4
        with:
          repository: ankitects/anki
          path: ankitects-anki
          submodules: true

      - name: Setup interactive ssh session
        uses: Warpbuilds/action-debugger@v1.3

      #- name: Change directory to anki
      #  run: cd ${{ github.workspace }}/ankitects-anki/

      #- name: Build anki-sync-server with release profile
      #  run: |
      #    PROTOC=$(which protoc) \
      #    RUSTFLAGS='-C target-feature=+crt-static'\
      #    cargo build --release --target x86_64-unknown-linux-gnu --package anki-sync-server

  #docker-and-push:
